---
name: project-init
description: "プロジェクト立ち上げの対話型スキャフォールダー。要件・フォルダ構成・CLAUDE.md の内容を段階的にインタビューし、その回答をクリティカルシンキング（前提の検証・矛盾・抜け）とラテラルシンキング（別解・逆転・他分野からの転用）でレビューしてブラッシュアップしてから、フォルダ構成、要件定義書、CLAUDE.md、プロジェクト用スキル、サブエージェント、プラグイン雛形、MCP 設定（.mcp.json）を一括生成する。ユーザーが「プロジェクトを始めたい」「新規リポジトリのセットアップ」「CLAUDE.md を作りたい」「要件を整理してから着手したい」「Claude Code 用の構成を整えたい」と言ったとき、/project-init と打ったとき、あるいは空または構成が未整備のディレクトリで開発を始めようとしているときに必ず使う。既存プロジェクトの Claude Code 構成の見直しにも使う。Interactive project bootstrap — interview requirements, folder layout and CLAUDE.md needs, review them critically and laterally, then scaffold docs, CLAUDE.md, skills, subagents, plugin and MCP config."
---

# project-init — 対話で要件を固めてからプロジェクトを立ち上げる

このスキルの目的は、「とりあえず作り始めて後から後悔する」を防ぐことだ。要件・構成・Claude への指示を **書き出す前に一度疑い、別の角度から見直す** 工程を挟み、その結果を実際のファイル群として着地させる。

5 フェーズを順番に進める。前のフェーズの合意なしに次へ進まない。

| フェーズ | やること | 詳細 |
|---|---|---|
| ① 要件 | 何を・誰のために・なぜ作るかを聞く | `references/questions.md` §1 |
| ② フォルダ構成 | 技術スタックと配置方針を聞く | `references/questions.md` §2 |
| ③ CLAUDE.md | Claude に守らせたいルール・文脈を聞く | `references/questions.md` §3 |
| ④ レビュー | ①〜③をクリティカル／ラテラルの両輪で検証し、改訂案を提示して合意を取る | `references/review-lenses.md` |
| ⑤ 生成 | フォルダ、要件定義書、CLAUDE.md、skills、agents、plugin、.mcp.json を作る | `references/scaffold-guide.md` |

## 始める前に

1. **現状を見る。** `ls -la`、`git status`、既存の `CLAUDE.md` / `.claude/` / `README` / `package.json` 等を確認する。
2. **`$ARGUMENTS`（`/project-init <プロジェクト名や一言説明>`）があれば初期仮説にする。** 「/project-init 家計簿アプリ」なら、家計簿アプリを前提に①の質問を具体化する。
3. **言語はユーザーに合わせる。** 質問・レビュー・生成物のコメントはユーザーが使っている言語で書く。ファイル名・識別子は英語。
4. **AskUserQuestion ツールがあれば使う。** なければプレーンテキストで質問する。どちらの場合も 1 ラウンドあたり 3〜4 問まで。選択肢を提示できる質問は選択肢にし、自由回答が必要なものだけ自由回答にする。回答が薄いときは「例えば〜ですか？」と具体例で掘る。
5. **規模に合わせて圧縮する。** 個人用ツールや小規模プロジェクト（利用者が本人だけ、外部連携なし、期限が数週間以内）なら、①〜③を各 1 ラウンドに圧縮してよい。その場合も③の 3 ループ（検証・レビュー・フィードバック）と、④のレビューは省かない。

### 既存プロジェクトの場合

- 観察結果（スタック、コマンド、ディレクトリ、既存 CLAUDE.md の内容）を先に箇条書きで提示し、「この理解で合っているか」を最初の質問にする。分かっていることを聞き直さない。
- 既存 CLAUDE.md は「残す行／削る行／書き換える行」に分類して差分を示し、確認を取ってから書き換える。今も有効な行は残す。
- 既存ファイルの移動・リネーム（`src/app.tsx` → `src/app/page.tsx` 等）は **提案にとどめ、自分ではやらない**。動いているコードを構成の都合で壊すリスクは、このスキルの責任範囲外。
- ⑤の生成順は同じだが、既にあるもの（`.gitignore`、README）は追記で済ませる。

## ① 要件を質問する

`references/questions.md` §1 の質問群から、このプロジェクトに関係するものを選んで聞く。全部聞く必要はない。目的は次の 6 点が埋まることだ：

- 解決したい課題と、それを抱えている人（ユーザー像）
- スコープの中と外（「やらないこと」を必ず 1 つ以上取る）
- 成功の定義（何ができたら「完成」か、測れる形で）
- 制約（期限、予算、既存システム、法規、チーム規模、スキルセット）
- 非機能要件（性能、セキュリティ、可用性、運用、i18n）で無視できないもの
- 想定リスクと、まだ決まっていないこと

回答はそのまま書き留める。この時点では評価・修正しない（評価は④でまとめてやる。途中で口を挟むとユーザーの思考が途切れる）。

## ② フォルダ構成を質問する

`references/questions.md` §2。聞くのは「決め」であって、ディレクトリ名の一つ一つではない：

- 言語／フレームワーク／パッケージマネージャ／ビルド・テストツール
- モノレポか単一パッケージか、フロント／バック／インフラの分離
- レイヤリング方針（機能別 vs 種類別、ドメイン層を切るか）
- テストの置き場所（コロケーション vs `tests/`）、ドキュメントの置き場所
- 既存の規約や参考にしたいリポジトリがあるか

回答を受けたら **暫定ツリー** をコードブロックで示し、認識合わせをする。ただしここでも確定させない。④で見直す。

## ③ CLAUDE.md に必要なことを質問する

`references/questions.md` §3。CLAUDE.md は毎セッション読み込まれるので、**入れるべきなのは「Claude が自力では知り得ない・推測を誤りやすいこと」だけ** だ。この観点をユーザーに共有した上で聞く：

- よく使うコマンド（build / test / lint / dev / deploy）と、実行してはいけないコマンド
- コーディング規約のうち、リンターで機械的に検出できないもの
- アーキテクチャ上の境界（触ってよい層・ダメな層、依存の向き）
- ドメイン用語の定義（チーム内の言葉と一般語の対応）
- Git 運用（ブランチ命名、コミットメッセージ、PR の作法）
- Claude に期待する振る舞い（確認を取ってから進めるべき操作、応答言語、説明の粒度、タスク単位で頼むかゴール単位で任せるか）
- **検証・レビュー・フィードバック収集の 3 ループ** — 変更が「動いた」と何で確かめるか、誰が／何が変更を見直すか、使った人の声をどこから拾うか（`references/questions.md` §3-5）。**この 3 つは必ず聞く。** Claude に任せる範囲を広げるほど、人の目の代わりにこの 3 ループが品質を支える
- 別ファイルに分けたいルール（`.claude/rules/*.md` にパス限定で置くべきもの）

「ここに書く必要はない」と判断できる回答（例：一般的な言語仕様、リンターが強制する規約）は、その場で理由を添えて「外す候補」として印を付けてよい。ただし採否の判断は④でまとめて取る（①で「途中で評価しない」と決めたのと同じ理由。ここで許すのは CLAUDE.md の性質に関する事実の共有までで、ユーザーの要望の良し悪しの評価ではない）。

## ④ クリティカル／ラテラルシンキングでレビューする

①〜③の回答を **一つの文書として並べ**、`references/review-lenses.md` のレンズを順に当てる。ここがこのスキルの価値の中心なので、時間をかける。

**クリティカル（守りの検証）** — 前提は本当か、矛盾はないか、抜けはないか、測れるか、YAGNI に反していないか、要件と構成と CLAUDE.md の三者が整合しているか。
**ラテラル（攻めの別解）** — 逆にしたら、極端にしたら、他分野の解き方を借りたら、制約を外したら／増やしたら、既存のものを組み合わせたら、作らずに済ませたら。

やり方：

1. 各レンズから見つけた指摘を **「指摘 → 根拠 → 提案」** の形で書き出す。数を稼ぐより、決定を変えうる指摘を優先する。5〜10 件程度に絞る。
2. 指摘を「① 要件」「② 構成」「③ CLAUDE.md」に紐づけ、**改訂後の要件サマリー・改訂後ツリー・CLAUDE.md の目次案** を一枚のレビューレポートにまとめて提示する（レポート形式は `references/review-lenses.md` §C）。提示前に同 §D の自己点検を通す。
3. ユーザーに **採用／不採用／保留** を指摘ごとに選んでもらう。全採用を前提にしない。ラテラルな提案は「面白いが今回はやらない」と断られるのが普通で、それでよい。断られた案は要件定義書の「検討したが採用しなかった選択肢」に残す（後で同じ議論を繰り返さないため）。
4. 判断を反映した **確定版**（要件サマリー・ツリー・CLAUDE.md 目次・却下した選択肢・未決事項）を短く提示し、⑤に進む。

## ⑤ フォルダ構成・要件定義書・CLAUDE.md・skills・agents・plugin・MCP を生成する

`references/scaffold-guide.md` に各成果物のテンプレートと Claude Code 側の仕様（frontmatter のフィールド名など）がある。生成前に必ず読む。

このスキルが作るのは **構成・ドキュメント・Claude Code の設定** であって実装ではない。ソースコードのファイルは作らないか、作るならモジュールの責務を 1〜3 行書いた docstring だけのスタブにとどめる。実装を始めるのはスキル終了後、ユーザーの指示で。

「作らない」と判断した成果物（plugin、MCP、agent、hooks 等）は、**作らない理由を一言添えて** 最後の報告に含める。黙って省くとユーザーは忘れたのか判断したのか分からない。

生成順：

1. **フォルダ構成** — 合意したツリーを作る。`scripts/scaffold.py` に JSON マニフェストを渡せば一括作成してツリーを表示できる（`python3 ${CLAUDE_SKILL_DIR}/scripts/scaffold.py manifest.json`。`--dry-run` で確認してから実行できる）。既存ファイルは上書きしない。`.gitkeep` は既定では置かれないので、全ファイルを書き終えた最後に空ディレクトリへまとめて置く（`find . -type d -empty -not -path './.git/*' -exec touch {}/.gitkeep \;`）。
2. **要件定義書** `docs/requirements.md` — ④で確定した内容。「検討したが採用しなかった選択肢」「未決事項」の節を必ず含める。
3. **CLAUDE.md** — ③と④の結果。**100〜200 行以内**。長くなるなら `.claude/rules/<topic>.md` にパス限定（`paths:` frontmatter）で分割する。要件定義書は `@docs/requirements.md` で参照するのではなく、要点だけ転記する（要件定義書は長く、毎セッション全文読ませる価値はない）。③で聞いた 3 ループは「完了の定義（検証）」節に落とす。**今のモデルやツールの限界に合わせた一時的な対処**（「大きなファイルは分割して読ませる」「〜は苦手なので毎回確認する」等）を書くときは、その行に `<!-- 賞味期限: YYYY-MM -->` を付ける。モデルやツールが更新されれば不要になる指示を、恒久ルールと混ぜない。
4. **skills** `.claude/skills/<name>/SKILL.md` — この要件で **繰り返し発生する定型作業** にだけ切る。例：DB マイグレーション手順、リリース手順、特定フォーマットのドキュメント生成。③で 3 ループが決まったなら「PR 前チェック」「フィードバック巡回」が候補の筆頭になる。1〜3 個が目安。汎用的すぎるもの（「コードを書く」）は作らない。
5. **agents** `.claude/agents/<name>.md` — **メインの文脈を汚したくない・権限を絞りたい・並列化したい** 役割にだけ切る。例：読み取り専用のコードレビュアー、テスト実行専任。作る根拠を各エージェントの description に書く。**ファンアウト → 反証レビュー構成**（観点別の複数エージェントで指摘を出し、別エージェントが反証する）は、**ユーザーが③で明示的に求めたときだけ** 作る。既定では作らず、こちらから提案もしない（`references/scaffold-guide.md` §5）。
6. **plugin** `.claude-plugin/plugin.json` ほか — **複数プロジェクトで再利用する見込みがあるとき** だけ作る。単一プロジェクトなら skills/agents を直接 `.claude/` に置いて終わりにし、plugin は作らない旨を説明する。
7. **MCP** `.mcp.json` — 要件から必要な外部接続（DB、GitHub、ブラウザ、Figma、Slack、社内 API 等）を洗い出し、候補を **「何に使うか／なくても困らないか」付きで** 提示し、採用されたものだけ書く。外部接続がない、または全候補が見送られたなら `.mcp.json` は作らず、見送った候補を要件定義書の「検討したが採用しなかった選択肢」に残す。秘密情報は `${ENV_VAR}` 参照にし、`.env.example` に変数名を列挙する（参照する変数がなければ `.env.example` も作らない）。存在を確認できないサーバー名・パッケージ名を捏造しない。不確かなら公式レジストリや `claude mcp add` の案内にとどめる。
8. **settings** `.claude/settings.json` — ③で聞いた「確認してほしい操作」を `ask`、「してはいけない操作」を `deny` に落とす。hooks は必要なときだけ（例：編集後に自動フォーマット）。`settings.local.json` は `.gitignore` へ。

### 生成後の検証（省略しない）

`python3 ${CLAUDE_SKILL_DIR}/scripts/validate.py` を実行する。次の 4 点を一度に確認し、問題があれば exit 1 で一覧を出す：

- ツリーを表示する → 合意したものと目視で突き合わせる。
- 全 SKILL.md / agents / rules の frontmatter が有効な YAML で、`name` がディレクトリ名（agents はファイル名）と一致し、`description` が空でない。
- 作った JSON（`.mcp.json` / `plugin.json` / `settings.json`）が構文的に正しい。
- CLAUDE.md が 200 行以内（超えていれば `.claude/rules/` への分割を提案）。

スクリプトが使えない環境では、同じ 4 点を `find` / `python3 -c "import yaml,..."` / `python3 -m json.tool` / `wc -l` で手動確認する。
- 最後に、**次にユーザーがやること**（環境変数の設定、`claude plugin validate`、初回コミット、未決事項の期限、CLAUDE.md の賞味期限付きの行の **見直し時期**）を 3〜5 行で示す。

## やってはいけないこと

- 質問をまとめて 10 問以上投げる。回答疲れで質が落ちる。
- ④を「良いと思います」で済ませる。指摘ゼロのレビューはレビューではない。少なくとも「削れるもの」を一つ探す。
- ユーザーの決定をレビューで勝手に上書きする。提案は提案として提示し、選ばせる。
- 存在しない MCP サーバー、frontmatter フィールド、CLI オプションを書く。仕様は `references/scaffold-guide.md` を優先する。同ガイドで「要確認」と付いている項目は **生成物に書かず**、ユーザーへの案内（「公式ドキュメントで確認の上、必要なら追加」）にとどめる。
- 既存ファイルを無言で上書きする。差分を示して確認を取る。
- ③で検証・レビュー・フィードバック収集の 3 ループを **一つも聞かずに** ⑤の生成へ進む。Claude に任せる範囲だけ決めて、確かめる手段を決めないのは片手落ち。
- 今のモデル・ツールの限界に合わせた回避策を、賞味期限なしの恒久ルールとして CLAUDE.md に書く。特定のツールや手順に執着させる指示も同じ。
- ユーザーが求めていないのに、ファンアウト構成（複数エージェントによる反証レビュー）を作る、または提案する。③で明示的に求められたときだけ作る。
